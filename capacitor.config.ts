import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IONIC-FIREBASE-AUTH-FILE-UPLOAD',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
